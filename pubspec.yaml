workflows:
  android-build:
    name: Android Build
    max_build_duration: 60

    environment:
      flutter: stable

    scripts:
      - echo "Start build"
      - unzip android.zip
      - cd android
      - ./gradlew assembleRelease

    artifacts:
      - android/app/build/outputs/**/*.apk
